# Database — M4

PostgreSQL models, session/configuration, repositories and Alembic migrations belong here. M3 calls this layer through repository/service interfaces; M4 must not reinterpret M2 compliance decisions.