# Schemas — M3

Request/response models shared at the API boundary. Keep these aligned with `docs/api-contract.md`. Domain-owned models remain in their owning module unless they are intentionally exposed as API schemas.